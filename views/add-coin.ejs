<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Add Coin - CoinMarket</title>
  <link rel="icon" href="/favicon.ico" type="image/x-icon" />
  <style>
    /* Reset & Base */
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; background-color: #f5f5f5; color: #111; padding-bottom: 4rem; }
    a { text-decoration: none; color: inherit; }

    /* Header & Navbar */
    .header { text-align: center; padding: 2rem 1rem; }
    .logo { font-size: 2rem; font-weight: bold; color: #1e88e5; }
    .navbar { display: flex; justify-content: center; gap: 2rem; background: #fff; box-shadow: 0 1px 4px rgba(0,0,0,0.1); }
    .navbar__item { padding: 1rem 0; font-size: 1rem; position: relative; }
    .navbar__item:hover::after, .navbar__item--active::after { content: ''; position: absolute; bottom: 0; left: 0; width: 100%; height: 2px; background: #1e88e5; }

    /* Form Styles */
    .container { max-width: 600px; margin: 2rem auto; padding: 0 1rem; }
    .form-card { background: #fff; border-radius: 8px; box-shadow: 0 2px 8px rgba(0,0,0,0.05); padding: 2rem; }
    .form-group { margin-bottom: 1.5rem; }
    label { display: block; margin-bottom: 0.5rem; color: #333; font-weight: 500; }
    input[type="text"], input[type="url"], textarea { width: 100%; padding: 0.75rem; border: 1px solid #ccc; border-radius: 4px; font-size: 1rem; font-family: inherit; }
    textarea { resize: vertical; min-height: 100px; }
    .recommendation { font-size: 0.85rem; color: #555; margin-top: 0.5rem; margin-bottom: 1rem; padding-left: 0.25rem; white-space: normal; word-wrap: break-word; }
    .recommendation a { color: #1e88e5; }
    .checkbox-group { display: flex; flex-wrap: wrap; gap: 0.5rem 1rem; }
    .checkbox-group label { display: flex; align-items: center; font-size: 1rem; }
    .checkbox-group input[type="checkbox"] { margin-right: 0.5rem; }
    .btn { background: #1e88e5; color: #fff; border: none; padding: 0.75rem 1.5rem; border-radius: 4px; font-size: 1rem; cursor: pointer; transition: background 0.2s; }
    .btn:hover { background: #1565c0; }

    /* Footer */
    .footer { position: fixed; bottom: 0; left: 0; width: 100%; background: #fff; box-shadow: 0 -1px 4px rgba(0,0,0,0.1); padding: 1rem 0; text-align: center; font-size: 0.9rem; color: #666; }
    .footer a { color: #1e88e5; }
  </style>
</head>
<body>
  <!-- Header -->
  <header class="header">
    <div class="logo">CoinMarket</div>
  </header>

  <!-- Navbar -->
  <nav class="navbar">
    <a href="/" class="navbar__item">Home</a>
    <a href="/coins" class="navbar__item navbar__item--active">Coins</a>
    <a href="/categories/new" class="navbar__item">Add Category</a>
    <a href="/coins/new" class="navbar__item">Add Coin</a>
  </nav>

  <!-- Main Form -->
  <main class="container">
    <div class="form-card">
      <h1 style="margin-bottom:1.5rem; color:#333;">Add New Coin</h1>
      <form action="/coins/add" method="post">
        <div class="form-group">
          <label for="coinName">Coin Name</label>
          <input type="text" id="coinName" name="coinName" required>
        </div>
        <div class="form-group">
          <label for="coinSymbol">Symbol</label>
          <input type="text" id="coinSymbol" name="coinSymbol" required>
        </div>
        <div class="form-group">
          <label for="logoUrl">Logo URL</label>
          <input type="url" id="logoUrl" name="logoUrl">
          <div class="recommendation">
            Find free images on <a href="https://unsplash.com" target="_blank">Unsplash</a>, <a href="https://www.pexels.com" target="_blank">Pexels</a>, or <a href="https://pixabay.com" target="_blank">Pixabay</a>.
          </div>
        </div>
        <div class="form-group">
          <label for="bannerUrl">Banner URL</label>
          <input type="url" id="bannerUrl" name="bannerUrl">
          <div class="recommendation">
            You can also source banners from the same sites above.
          </div>
        </div>
        <div class="form-group">
          <label>Select Categories</label>
          <div class="checkbox-group">
            <% categories.forEach(category => { %>
              <label>
                <input type="checkbox" name="categoryIds" value="<%= category.id %>">
                <%= category.name %>
              </label>
            <% }) %>
          </div>
        </div>
        <div class="form-group">
          <label for="coinDesc">Description</label>
          <textarea id="coinDesc" name="coinDesc"></textarea>
        </div>
        <button type="submit" class="btn">Save Coin</button>
      </form>
    </div>
  </main>

  <!-- Footer -->
  <footer class="footer">
    This project is a part of <a href="https://www.theodinproject.com/lessons/node-path-nodejs-inventory-application" target="_blank" rel="noopener">The Odin Project</a>.<a href="https://github.com/onurdemirk/coin-market" target="_blank" rel="noopener"> Check out the project repository here</a>.<br>
    © onr - code is poetry, 2025
  </footer>
</body>
</html>
